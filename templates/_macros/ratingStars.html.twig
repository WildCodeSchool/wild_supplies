{% macro ratingStars(rating) %}
	<div class="ratingStars">
		{% for i in range(1, 5) %}
			{% if rating < i %}
				<img class="star" src="{{pathSvg}}star.svg"/>
			{% else %}
				<img class="star" src="{{ asset('build/svg/starChecked.svg')}}"/>
			{% endif %}
		{% endfor %}
	</div>
{% endmacro %}
